package com.paymybuddy.webapp.service;

public class TransferServiceImpl {

	// **************************** TODOs LIST ***********************************
	
	// Method: 
	// --> getAllTransfers(): List(TransfersDTO) served by TransferMapper.toDTO(transfer)
	// --> getAllTransfersByUser(UserDTO); TransferDTO by pages
	// --> addTransfer(transferDTO): transferDTO
	// --> pagination of transfer list
	// --> Before validation of Transfer, Transaction approval done by checking the account balance amount
	
	
}
